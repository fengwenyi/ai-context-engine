# 数据库设计文档

> 用途：描述模块涉及的所有表、字段、索引、关系。

---

## 1. 概述
- 涉及的业务范围
- 涉及的表清单

## 2. 实体关系
- ER 图（或文字描述）

## 3. 表定义

### 表名：`{表名}`
**说明**：xxx

| 字段 | 类型 | 允许空 | 默认 | 索引 | 说明 |
|---|---|---|---|---|---|
| id | bigint unsigned | N | — | PK | 主键 |
| created_at | datetime | N | CURRENT_TIMESTAMP | — | 创建时间 |
| updated_at | datetime | N | CURRENT_TIMESTAMP | — | 更新时间 |
| ... | ... | ... | ... | ... | ... |

**索引**：
- PRIMARY KEY (`id`)
- INDEX `idx_xxx` (`field1`, `field2`)
- UNIQUE KEY `uk_xxx` (`field`)

**关系**：
- `field_id` 外键到 `other_table.id`

### 表名：`{表名2}`
（同上）

## 4. 关键查询
- 列出对索引设计有影响的高频/慢查询

## 5. DDL 文件
建表/变更 SQL 保存在：`features/{x}/outputs/sql/`

## 6. 数据迁移（如有）
- 历史数据处理方案
- 回滚策略
