# 改动方案

> 用途：实现代码前，先输出本方案，等用户确认。
> 可写在功能主文档内的"改动方案"区，也可独立成文。

---

## 1. 影响范围
- 涉及模块：xxx
- 涉及端：后端 / 管理端 / 用户端 / 小程序 / App
- 依赖服务：xxx

## 2. 涉及文件
**新增**：
- `path/to/new_file.ext`

**修改**：
- `path/to/existing_file.ext`：改动点摘要

**删除**：
- `path/to/old_file.ext`：删除原因

## 3. 接口变化
**新增**：
| 方法 | 路径 | 用途 |
|---|---|---|
| POST | /api/v1/xxx | xxx |

**修改**：
| 方法 | 路径 | 变更点 | 兼容性 |
|---|---|---|---|
| GET | /api/v1/xxx | 响应新增 field | 向后兼容 |

## 4. 表结构变化
**新增表**：
```sql
CREATE TABLE xxx (...);
```

**修改表**：
```sql
ALTER TABLE xxx ADD COLUMN ...;
```

**索引变更**：
- 新增 / 删除 / 调整

## 5. 配置变化
- 新增/修改的配置项（含默认值）

## 6. 风险点
- 兼容性：xxx
- 数据迁移：xxx
- 性能：xxx
- 权限：xxx
- 回滚策略：xxx

## 7. 实施顺序
1. 表结构变更（DDL 上线）
2. 后端接口实现
3. 接口文档输出
4. 管理端 / 用户端 / 小程序 实现
5. 联调
6. 上线

## 8. 验证方式
- 自测：xxx
- 联调：xxx
- 回归：xxx
