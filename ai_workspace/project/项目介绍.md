# 项目介绍

> 本文件是项目的"门牌"，AI 每次开工前必读。
> 告诉 AI：项目长什么样、有哪些端、代码在哪、关键约定是什么。
>
> 新项目套用时，请把下面的占位内容替换为本项目实际信息。

---

## 1. 项目概要

- **项目名称**：{xxx}
- **一句话定位**：{xxx}
- **目标用户**：{xxx}
- **核心价值**：{xxx}
- **当前阶段**：设计中 / 开发中 / 已上线（具体描述）

---

## 2. 端清单

> 列出本项目所有端，每个端单独一节。AI 会根据这里找到代码位置。

### 后端
- **代码路径**：`{apps/xxx-server}`
- **技术栈**：{语言 / 框架 / ORM / 数据库}
- **启动命令**：`{xxx}`
- **配置位置**：`{config/}`

### 管理端
- **代码路径**：`{apps/xxx-admin}`
- **技术栈**：{框架 / UI 库}
- **是否动态路由**：是 / 否
- **启动命令**：`{xxx}`

### 用户 Web 端
- **代码路径**：`{apps/xxx-web}`
- **技术栈**：{xxx}
- **启动命令**：`{xxx}`

### 微信小程序
- **代码路径**：`{apps/xxx-wechat-mini}`
- **技术栈**：{原生 / Taro / uni-app}
- **启动命令**：`{xxx}`

### App
- **代码路径**：`{apps/xxx-app}`
- **技术栈**：{React Native / Flutter / 原生}
- **启动命令**：`{xxx}`

> 没有的端整节删除即可。

---

## 3. 仓库结构

```
项目根/
├── apps/                  # 多端代码
│   ├── xxx-server/
│   ├── xxx-admin/
│   └── ...
├── packages/              # 共享包（如有）
├── docs/                  # 工程级文档（如有）
├── ai_workspace/          # AI 协作工作区
└── ...
```

---

## 4. 关键代码结构（按端补充）

### 后端
```
apps/xxx-server/
├── cmd/                   # 入口
├── internal/
│   ├── handler/           # 路由处理
│   ├── service/           # 业务逻辑
│   ├── repo/              # 数据访问
│   └── model/             # 实体
├── pkg/                   # 可复用组件
└── config/                # 配置
```

### 管理端
```
apps/xxx-admin/
├── src/
│   ├── views/             # 页面
│   ├── api/               # 接口调用
│   ├── router/            # 路由（含动态路由配置）
│   └── components/        # 组件
└── ...
```

> 各端按实际情况补充。

---

## 5. 项目特定约定

- **数据库表前缀**：`{xxx_}`（如有）
- **接口路径前缀**：`/api/v1`
- **鉴权方式**：{Header / Cookie}，Token 字段：`{xxx}`
- **错误码段位**：4xxx 客户端 / 5xxx 服务端 / {自定义业务段}
- **环境**：dev / test / pre / prod
- **特殊命名约定**：{xxx}

---

## 6. 外部依赖

| 依赖 | 用途 | 文档/链接 |
|---|---|---|
| {xxx} | {xxx} | {xxx} |

---

## 7. 关键文档导航

- 产品设计：`project/designs/产品设计.md`
- 系统架构：`project/designs/系统架构.md`
- 数据库设计：`project/designs/数据库设计.md`
- 技术选型：`project/designs/技术选型.md`
- 接口文档总览：`project/apis/`

---

## 8. 联系人 / 责任人（可选）

- 产品：{xxx}
- 后端：{xxx}
- 前端：{xxx}
