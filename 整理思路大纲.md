# ai_workspace 方法论整理思路大纲

> 目标：把当前项目里长出来的 `ai_workspace` 实践，抽象成一套**可移植到任意新项目**的方法/框架，让 AI 在不同项目里都能以一致的方式协作开发。

---

## 一、目标与原则

### 1.1 目标
- **可移植**：一套结构 + 一组模板，复制到任何新项目即可启用。
- **分层清晰**：方法论本体（通用）⇄ 项目落地（特定）分开。
- **AI 友好**：单一入口 `AI_CONTEXT.md`，AI 读完入口就知道去哪找规范、模板、历史。
- **覆盖全生命周期**：从"无项目"到"项目内迭代"全部场景都有路径可走。
- **沉淀可复用**：让 AI 在过程中沉淀 QA、决策、理解，下次少走弯路。

### 1.2 设计原则
- **入口唯一**：所有 AI 协作的起点只有 `AI_CONTEXT.md`。
- **约定优于配置**：目录结构、命名规则固定，AI 不用猜。
- **模板即规范**：所有交付物（PRD、设计文档、接口文档等）都有模板，AI 照填即可。
- **迭代留痕**：每个功能、每次改动都有可追溯的文档与日志。
- **方法 vs 实例分离**：通用模板放"框架层"，项目特定的规范放"项目层"。

---

## 二、现状分析（当前 ai_workspace）

### 2.1 现有结构
```
ai_workspace/
├── AI_CONTEXT.md            # 入口（新版，简版，只列目录与规范）
├── ai_context_V1.md         # 入口（旧版，含"改动方案先行"流程）
├── docs/
│   ├── ai_files/            # AI 自存：QA、理解、决策（自治）
│   ├── apis/                # 接口文档（空）
│   ├── designs/             # 设计文档（空）
│   ├── specs/               # 规范（后端/管理端/小程序/UI/web端/表前缀/技术选型）
│   └── templates/           # 模板（PRD/接口/数据库/系统设计/联调清单）
└── features/
    ├── prompt_template.md   # 迭代/执行提示词模板
    ├── feature_xxx/         # 空壳示例
    └── 109-xxx/             # 真实功能示例（迭代-01 完整结构）
```

### 2.2 优点（保留）
- 入口文件 + 自治 `ai_files` 思路很轻量，AI 容易遵循。
- features 按"编号-名称"组织 + 迭代式提示词，结构清晰。
- specs / templates 分离合理。

### 2.3 问题（待解决）
1. **两个入口文件并存**：`AI_CONTEXT.md`（新简版）和 `ai_context_V1.md`（旧详版）信息分散，需要合并。
2. **方法与项目混在一起**：`specs/` 里的"后端开发规范、小程序规范"是项目特定的，但和"模板、流程"放在一个目录，移植时不好剥离。
3. **场景覆盖不全**：当前主要服务"功能迭代"（场景 3），对"从 0 设计项目"（场景 1）、"新系统从设计到联调"（场景 2）缺少明确流程与产物清单。
4. **11 个开发动作没有显式映射**：项目设计、数据库设计、接口设计、UI 提示词、接口联调、SQL 文件……每个动作的"输入/输出/模板/沉淀位置"没有一张表说清。
5. **`docs/apis`、`docs/designs` 是空的**：约定了位置但没说"何时写、谁写、写到什么程度"。
6. **`ai_files` 自治规则模糊**：什么该存、什么不该存、命名规则、什么时候清理，没有明确指引。
7. **多端项目的组织缺指引**：后端、管理端、小程序、APP 的产物放在哪、怎么交叉引用，没说清。
8. **缺少"骨架初始化"工具**：新项目要套用这套方法时，没有一键脚本或清单。

---

## 三、提议的新结构（草案）

### 3.1 顶层结构
```
ai_workspace/
├── AI_CONTEXT.md            # ★ 唯一入口（合并 V1 + 现版）
├── method/                  # 方法论本体（通用，跨项目复用）
│   ├── workflows/           # 三类场景的标准流程
│   │   ├── 01-项目设计.md           # 场景 1：从无到有
│   │   ├── 02-新系统开发.md         # 场景 2：项目内新模块
│   │   └── 03-功能迭代.md           # 场景 3：增量/bug
│   ├── templates/           # 通用模板
│   │   ├── PRD 模板.md
│   │   ├── 系统设计模板.md
│   │   ├── 数据库设计模板.md
│   │   ├── 接口设计模板.md
│   │   ├── 接口文档模板.md
│   │   ├── UI 提示词模板.md
│   │   ├── 联调清单模板.md
│   │   ├── 功能迭代模板.md
│   │   └── CHANGELOG 模板.md
│   └── prompts/             # 通用提示词
│       ├── 入口提示词.md
│       ├── 迭代提示词.md
│       └── 改动方案提示词.md
├── project/                 # 项目特定内容（每个项目自填）
│   ├── specs/               # 项目自己的开发规范（技术栈、命名、风格）
│   ├── designs/             # 项目整体设计产物（产品/系统/数据库）
│   ├── apis/                # 接口文档成品
│   └── ai_files/            # AI 自治笔记（QA / 理解 / 决策）
└── features/                # 按功能/迭代组织
    └── {编号}-{名称}/
        ├── {编号}-{名称}.md     # 功能主文档（含迭代）
        ├── CHANGELOG.md         # 变更日志
        └── outputs/             # 该功能的交付物（SQL、路由配置、UI 提示词等）
```

> 关键拆分：`method/`（搬到新项目几乎不动）⇄ `project/`（每个项目自己长内容）。

### 3.2 入口文件（`AI_CONTEXT.md`）应该包含什么
1. **使命**：AI 协作的规则（先方案后实现、单一入口、模板优先）。
2. **场景路由**：判断当前任务属于场景 1 / 2 / 3，指向对应 workflow。
3. **目录地图**：method / project / features 各自作用与导航。
4. **常用文件快捷索引**：规范、模板、提示词的直达链接。
5. **AI 自治规则**：什么时候往 `project/ai_files` 写、命名规范、清理时机。
6. **沉淀义务**：功能完成后必须更新的清单（CHANGELOG、功能清单、ai_files）。

---

## 四、三类场景的工作流（要详细写的部分）

### 4.1 场景 1：项目设计（从 0 到 1）
**触发**：还没项目，AI 辅助完成产品/技术从 0 设计。
**步骤**：
1. 产品业务流程梳理 → 输出到 `project/designs/产品业务流程.md`
2. 产品形态/闭环/运营 → `project/designs/产品设计.md`
3. 技术选型（后端/管理端/APP/小程序）→ `project/designs/技术选型.md`
4. 整体架构 → `project/designs/系统架构.md`
5. 初始数据库设计 → `project/designs/数据库设计.md`
6. 初始化项目骨架（按选型）

**产物清单**：产品文档、技术选型、架构图、初版库表。

### 4.2 场景 2：新系统/新模块开发
**触发**：项目存在但该模块不存在，需要走完整设计→开发→联调闭环。
**步骤**：
1. 创建 `features/{编号}-{名称}/`
2. 模块设计（业务/数据/接口）→ 填模板，存功能目录或 `project/designs/`
3. 数据库设计 → 输出建表 SQL 到 `outputs/`
4. 接口设计 → 接口设计文档
5. 后端接口开发
6. 接口文档输出 → `project/apis/`
7. UI 提示词编写 → `outputs/ui-prompts/`（可用专业 UI AI 工具生成）
8. 前端页面开发（依据 UI 图）
9. 接口联调 → 填联调清单
10. 管理端动态路由配置（如有）→ `outputs/routes.json`
11. CHANGELOG + 功能清单更新

**产物清单**：设计文档、SQL、接口文档、UI 提示词、路由配置、联调清单、CHANGELOG。

### 4.3 场景 3：功能迭代 / Bug 修复
**触发**：系统已存在，只是增量改动。
**步骤**（沿用当前实践）：
1. 在已有 `features/{编号}-{名称}/` 下新增"迭代-xx"
2. 走"用户需求 → AI 疑问 → 需求梳理 → 拆分任务 → 任务状态"
3. **先出改动方案（影响范围/涉及文件/接口变化/表变化/风险/顺序），评审通过再动手**（从 V1 抢救回来）
4. 实现，按需出 SQL、路由、接口文档差异
5. 更新 CHANGELOG + 功能清单

---

## 五、11 个开发动作 → 模板/产物映射表（待整理时填详表）

| 动作 | 输入 | 输出 | 模板 | 落位 |
|---|---|---|---|---|
| 1. 项目整体设计 | 业务想法 | 产品&架构文档 | 产品设计/架构模板 | `project/designs/` |
| 2. 搭建项目 | 技术选型 | 工程骨架 | — | 各端代码仓库 |
| 3. 数据库设计 | 业务模型 | ER + 表结构 | 数据库设计模板 | `project/designs/` + `outputs/sql/` |
| 4. 接口设计 | 业务用例 | 接口列表+定义 | 接口设计模板 | features 内 / `project/apis/` |
| 5. 接口开发 | 接口设计 | 代码 | 后端规范 | 代码库 |
| 6. 接口文档 | 实现 | 文档 | 接口文档模板 | `project/apis/` |
| 7. UI 设计（提示词） | 页面清单 | UI 提示词 | UI 提示词模板 | `features/{x}/outputs/ui-prompts/` |
| 8. 页面开发 | UI 图 | 前端代码 | 前端规范 | 代码库 |
| 9. 接口文档（前端视角） | 联调需求 | 调用说明 | — | `project/apis/` |
| 10. 接口联调 | 接口文档 | 联调清单 | 联调清单模板 | `features/{x}/` |
| 11. SQL 文件 | 数据库设计 | DDL/DML | — | `features/{x}/outputs/sql/` |

> 补充：管理端动态路由配置、菜单/权限脚本等也归 `outputs/`。

---

## 六、AI 自治规则（`project/ai_files/`）

- **什么该存**：
  - 跨迭代有价值的"理解/决策/QA"（如某接口为什么这样设计）
  - 易踩坑、想避免下次重复问的事
  - 临时探索的中间结论（如调用链梳理）
- **什么不该存**：
  - 代码里能直接读到的信息
  - 一次性的、不会再用的内容
  - 与 git 历史重复的事
- **命名规则**：`{功能编号}-{话题简述}.md` 或 `topic-{主题}.md`
- **清理时机**：功能彻底完成后，过时的合并/归档

---

## 七、整理后会产出什么（执行清单）

执行整理时，将逐项完成：

1. **合并入口文件**：把 V1 与现版的内容融合，删掉 `ai_context_V1.md`，输出新 `AI_CONTEXT.md`。
2. **拆分目录**：建立 `method/` 与 `project/` 两层，迁移现有规范/模板。
3. **写三份 workflow 文档**：场景 1/2/3 标准流程。
4. **补齐缺失模板**：UI 提示词模板、CHANGELOG 模板、改动方案模板。
5. **改写提示词**：通用迭代提示词去项目化，独立成 `method/prompts/`。
6. **写映射表**：第五节那张"动作→模板→落位"表的完整版。
7. **写 `ai_files` 使用规则**：单独成文，作为 AI 自治的边界。
8. **写"新项目套用指南"**：复制 `method/` + 创建空 `project/` + 填入入口，三步走。
9. **示例保留**：保留 `109-整理内容不输出日志/` 作为场景 3 真实样例，`feature_xxx/` 作为空模板。

---

## 八、需要你确认的几个关键决策

在我开始整理前，请确认这几个问题（直接覆盖默认即可）：

1. **目录顶层是否采用 `method/` + `project/` 两层分离？**
   - 默认：是（强烈推荐，可移植性的关键）。
   - 备选：保持扁平（`docs/` + `features/`），靠命名约定区分。

2. **现有的项目特定规范（`docs/specs/后端开发规范.md` 等）如何处理？**
   - 默认：移到 `project/specs/`，保留原内容（它们是这个项目实际在用的）。
   - 备选：抽取通用部分到 `method/`，项目特定部分留 `project/`。

3. **`ai_context_V1.md` 的"改动方案先行"流程是否纳入新入口？**
   - 默认：纳入，作为场景 2/3 的强制前置步骤。

4. **功能编号规则**：当前是 `109-xxx`，需要规则化吗？
   - 默认：保留"递增编号-名称"，编号由用户分配（与外部任务系统对齐）。

5. **多端项目（后端/管理端/小程序/APP）的代码路径要不要在入口文件里固定？**
   - 默认：在 `AI_CONTEXT.md` 留"项目端清单"小节，由各项目自填（如当前的 `apps/windrunner-server` 等）。

6. **整理产出的文档语言**：全中文？
   - 默认：是（与现状一致）。

7. **是否要附带一个"新项目初始化脚本"**（PowerShell/Bash）一键拉骨架？
   - 默认：先不做，方法稳定后再加；先出文档版"套用指南"。

---

## 九、下一步

待你确认上面第八节的关键决策后，我会按本大纲执行整理，产出完整的新版 `ai_workspace/`（含 `method/`、`project/`、新版 `AI_CONTEXT.md`、三份 workflow、补齐的模板、映射表、自治规则、套用指南）。

如果有遗漏的场景或动作，请补充。
